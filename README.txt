Il faut d'abord installer le package socket.io à l'aide la commande : npm install

L'application se lance avec la commande : node app.js

On se connecte localement à l'adresse : http://localhost:8888/
